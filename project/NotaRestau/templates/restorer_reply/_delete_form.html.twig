<form method="post" action="{{ path('app_restorer_reply_delete', {'id': restorer_reply.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ restorer_reply.id) }}">
    <button class="btn">Delete</button>
</form>
