<form method="post" action="{{ path('app_user_review_delete', {'id': user_review.id}) }}" onsubmit="return confirm('Are you sure you want to delete this review?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_review.id) }}">
    <button class="btn btn-danger rounded-pill w-25">Delete</button>
</form>
